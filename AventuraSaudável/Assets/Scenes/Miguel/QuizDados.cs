/// <summary>
/// Classe estática que transporta os dados do quiz entre scenes.
/// Não precisa de ser colocada em nenhum GameObject — basta existir no projeto.
/// </summary>
public static class QuizDados
{
    public static int acertos       = 0;
    public static int totalPerguntas = 0;
}
